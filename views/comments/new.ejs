<% include ../partials/header %>
  <div class="container">
    <div class="row">
      <div class="col-md-3"></div>
      <div class="col-md-6">
        <h3>Add Comment to <em>"<%= campground.name %>"</em></h3>
        <hr>
        <div class="mx-auto my-4">
          <form action="/campgrounds/<%= campground._id %>/comments" method="post">
            <div class="form-group">
              <label for="desc">Comment</label>
              <textarea class="form-control" name="comment[text]" id="desc" rows="3"></textarea>
            </div>
            <div class="form-group">
              <button type="submit" class="btn btn-secondary btn-sm">Save</button>
              <a class="btn btn-danger btn-sm" href="/campgrounds/<%= campground._id %>">Cancel</a>
            </div>
          </form>
        </div>
      </div>
      <div class="col-md-3"></div>
    </div>
  </div>
<% include ../partials/footer %>